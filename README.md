# Dev Container

## Instructions
1. Build the `base` image and push to docker hub
2. Build the `cpp` image because it depends on `base`
3. Build the `py` image because it needs to compile python from the start
